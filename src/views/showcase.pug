extends index

block content
  container.container
    div.catsRow 
      each cat in cats
        container.catCard
          div #{cat.breed.name}
          a(href="/cats/"+cat.id)
            img(src=cat.imageUrl).img
